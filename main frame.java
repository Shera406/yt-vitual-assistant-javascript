import javax.swing.*;

public class MainFrame extends JFrame {
     public void initialize(){
       setTitle("AI-Assaint");
       setSize("500,600");
       setMinimumSize(new Dimension(300,400));
       setDefaultCloseOperation(WindowConsts)
     }
}
